server:
  port: 9000

spring:
  application:
    name: auth-server

  # H2 Database Configuration (for development)
#  datasource:
#    url: jdbc:h2:mem:authserver
#    driver-class-name: org.h2.Driver
#    username: sa
#    password:
#
#  # JPA Configuration
#  jpa:
#    hibernate:
#      ddl-auto: update
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true
#
#  # H2 Console (for development only)
#  h2:
#    console:
#      enabled: true
#      path: /h2-console

# Logging Configuration
logging:
  level:
    root: INFO
    org.springframework.security: DEBUG
    org.springframework.security.oauth2: DEBUG